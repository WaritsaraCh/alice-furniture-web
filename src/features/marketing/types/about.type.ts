export type TeamMember = {
  name: string;
  position: string;
  image: string;
  bio: string;
};

export type StoreLocation = {
  city: string;
  address: string;
  phone: string;
  hours: string;
  image: string;
};
